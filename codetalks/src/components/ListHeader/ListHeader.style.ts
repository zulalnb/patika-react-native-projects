import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    padding: 16,
    borderWidth: 1,
    borderStyle: 'dashed',
    borderColor: '#fff',
    borderRadius: 8,
    marginBottom: 16,
  },
  title: {
    fontSize: 20,
    fontWeight: '600',
    color: '#fff',
    textAlign: 'center',
  },
});
